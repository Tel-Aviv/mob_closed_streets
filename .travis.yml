language: node_js

node_js:
- '8.12.0'

sudo: false

cache:
  yarn: true

script:
  - yarn test

deploy:
  provider: releases
  skip-cleanup: true
  api_key: $GITHUB_TOKEN
#  github-token: $GITHUB_TOKEN
  file: "/dist/bundle.js"
#  keep-history: true
  on:
    branch: master
